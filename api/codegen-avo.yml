overwrite: true
schema:
  - ${GRAPHQL_URL_AVO}:
      headers:
        x-hasura-admin-secret: ${GRAPHQL_SECRET_AVO}
documents:
  # Only generate types for avo specific queries. This code should eventually be moved to the admin core package
  - "**/queries/avo/*.graphql"
  - "**/queries/avo/*.graphql"
  - "**/queries/avo/*.graphql"
generates:
  src/modules/admin-core/shared/generated/graphql-db-types-avo.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
