mutation insertTableColumnPreferencesForUser($profileId: uuid!, $columnKey: String!, $columns: jsonb!) {
	insert_users_table_column_preferences(objects: {profile_id: $profileId, key: $columnKey, values: $columns}) {
		affected_rows
		returning {
			id
			values
			key
			profile_id
		}
	}
}
