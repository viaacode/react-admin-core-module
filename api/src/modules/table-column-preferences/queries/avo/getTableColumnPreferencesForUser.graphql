query getTableColumnPreferencesForUser($profileId: uuid!, $columnKey: String!) {
	users_table_column_preferences(where: {profile_id: {_eq: $profileId}, url_key: {_eq: $columnKey}}) {
		id
		url_key
		visible_columns
		profile_id
	}
}
