query getFileByRepresentationSchemaIdentifier($id: String) {
	graph_representation(where: { id: { _eq: $id } }) {
		represents {
			dctermsFormat {
				dcterms_format
			}
		}
		includes {
			file {
				premis_stored_at
			}
		}
	}
}
