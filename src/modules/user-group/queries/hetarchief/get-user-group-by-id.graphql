query getUserGroupById($id: uuid!) {
	users_group(where: { id: { _eq: $id } }) {
		label
		id
		description
		created_at
		updated_at
		permissions(order_by: { permission: { label: asc } }) {
			permission {
				id
				name
				label
				description
			}
		}
	}
}
